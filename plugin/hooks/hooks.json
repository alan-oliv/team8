{
  "description": "Everything the console observes, plus the launcher that starts it. The observation hooks POST each event to the console on its default port through a curl one-liner that always exits 0, so a console that isn't running costs nothing but a discarded connection refusal instead of a hook-error notice on every tool call. A POST that finds nothing listening hands off to console-restart.sh, which brings the console back when a team is still live and stays silent either way. The launcher exits immediately unless a real team exists, except on the Workflow tool \u2014 a workflow forms no team, so that matcher points the console at the session that ran it instead. SessionStart also runs console-hint.sh, a pure systemMessage nudge toward /team8:console \u2014 it never starts the server itself, so a session that never spawns a team or workflow still learns the console exists without one running idle. Timeouts are in seconds.",
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 5 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 5
          }
        ],
        "matcher": "*"
      },
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/console-launch.sh\"",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "Workflow",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/console-launch.sh\"",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 5 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 5
          }
        ],
        "matcher": "*"
      },
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/console-launch.sh\"",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "Workflow",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/console-launch.sh\"",
            "timeout": 5
          }
        ]
      }
    ],
    "PermissionRequest": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 600 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 600
          }
        ],
        "matcher": "*"
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 5 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 5
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 5 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 5 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 5
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 5 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 5
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 5 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 5
          },
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/console-hint.sh\"",
            "timeout": 5
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 5 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 5
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "curl -sS -m 5 -X POST -H 'content-type: application/json' --data-binary @- http://127.0.0.1:4823/hook 2>/dev/null || OCTO_PORT=4823 \"${CLAUDE_PLUGIN_ROOT}/bin/console-restart.sh\"; exit 0",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
